<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= user.name%> </title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
    <h1 class="text-3xl"><span class="text-5xl font-serif">Hello,</span>  <%= user.name%> 🖐️</h1>
    <h5 class="mb-5">You can create a new post</h5>
    <div class="grid gap-4 m-4 ">
        <form action="/post" method="post">
            <div class="grid gap-4 m-4  ">
            <textarea placeholder="What is on your mind???" class="p-3 w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md"  name="content" ></textarea> 
            <input class="py-1 bg-blue-600 border-2 rounded-md w-48 text-xl" type="submit" value="Create New Post">
        </div> 
        </form> 
        
        <div class="posts mt-10">
            <h3 class="text-zinc-400">Your Posts</h3>
            <div class="postcontainer mt-5 ">
                <% user.posts.reverse().forEach(function(post){%>
                    <div class="post w-1/3 p-4 mb-4 border-[3px] border-zinc-800 rounded-md outline outline-4 outline-zinc-500 p-5">
                    <h4 class="text-2xl text-blue-400"><b> <%= user.name%></b></h4>
                    <p class="text-sm"><%= post.content%></p>
                    <div class="btns flex mt-5 gap-4">
                    <a class="text-blue-500" href="/like/<%=post._id%>"><%=post.likes.length%> ❤️Like</a>
                    <a class="text-zinc-500" href="/edit/<%=post._id%>">edit</a>
                </div>
                </div>
                <%})%>
            </div>
        </div>
    </div>
    <input type="submit" class="py-1 px-5 bg-red-600 border-2 rounded-md text-xl w-28" name="logout" value="Logout">
   
</div>
</body>
</html>